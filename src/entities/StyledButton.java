package entities;

import java.awt.*;

public class StyledButton extends Button {

    public StyledButton(int x, int y, int width, int heigth, int action, String text) {
        super(x, y, width, heigth, action, text);
    }

    @Override
    public void render(Graphics g) {

        super.render(g);
    }

    @Override
    public void action() {
        super.action();
    }
}
